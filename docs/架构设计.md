# 架构设计（类图与依赖）

> 本文随代码演进同步更新。✓ = 已实现；✗ = 未实现（标注所属实验）。期末报告"程序结构设计"章节以本文为底稿。

## 1. 分层总览

实线 = 当前已存在的依赖；虚线 = 未来模块接入后的依赖。

```mermaid
graph TB
    subgraph UI["UI 层　src/ui + main.cpp（交互与组装）"]
        MAIN["main.cpp ✓ 组装根"]
        WIN["Window ✓"]
        PC["PaintController ✓"]
    end
    subgraph ALG["算法层　（手写算法，禁止依赖 UI）"]
        RAS["Rasterizer ✓ 实验一"]
        FILL["Fill ✗ 实验二"]
        CLIP["Clip ✗ 实验二"]
        TRA["Transform ✗ 实验三"]
        BEZ["Bezier ✗ 实验四"]
        LIGHT["LightingRenderer ✗ 实验五·选做"]
        ANIM["Animation ✗ 期末项目"]
    end
    subgraph CORE["核心层　src/core（自足，仅依赖标准库）"]
        FB["FrameBuffer ✓"]
        CAN["Canvas ✓ 唯一 OpenGL 出口"]
        CP["Color / IPoint / FPoint ✓"]
    end

    MAIN --> WIN
    MAIN --> PC
    MAIN --> CAN
    MAIN --> FB
    PC --> RAS
    RAS --> FB
    CAN --> FB
    FB --> CP

    PC -. 实验二·多边形填充 .-> FILL
    PC -. 实验二·裁剪交互 .-> CLIP
    PC -. 实验三·选中图形变换 .-> TRA
    PC -. 实验四·控制点编辑 .-> BEZ
    ANIM -. 只调公开接口（红线） .-> PC
    ANIM -. 关键帧插值 .-> TRA
    FILL -.-> FB
    CLIP -. 剪后重画 .-> RAS
    TRA -.-> CP
    BEZ -.-> CP
    LIGHT -. 软渲染逐像素 .-> FB
```

## 2. 类图（按功能分包）

### 2.1 当前类图

按功能模块分色分块（core / raster / ui / app）。为保持图面清爽，**值类型的字段依赖不画**（如 `Shape` 含 `LineStyle`/`IPoint` 字段、`FrameBuffer` 用 `Color`），只画组装与调用关系；完整约束见第 3 节。

```mermaid
classDiagram
    direction TB

    namespace core {
        class Color {
            +uint8 r, g, b
            +operator==(o) bool
        }
        class IPoint {
            +int x, y
        }
        class FPoint {
            +float x, y
        }
        class FrameBuffer {
            +clear(color) void
            +putPixel(x, y, color) void
            +pixel(x, y) Color
        }
        class Canvas {
            +valid() bool
            +present(fb) void
        }
    }

    namespace raster {
        class LineStyle {
            +unsigned mask
            +int width
        }
        class Rasterizer {
            <<utility>>
            +drawLine(fb, a, b, color, style)
            +drawCircle(fb, center, r, color, style)
            +drawArc(fb, center, r, deg0, deg1, color, style)
        }
    }

    namespace ui {
        class Window {
            +shouldClose() bool
            +swapBuffers() void
            +pollEvents() void
            +setTitle(title) void
            +loader() LoaderFn
            +setOnKey(cb) void
            +setOnMouseButton(cb) void
            +setOnMouseMove(cb) void
        }
        class Shape {
            +Mode mode
            +IPoint a, b
            +int radius
            +float startDeg, endDeg
            +LineStyle style
        }
        class PaintController {
            +onKey(key, action) void
            +onMouseButton(button, action) void
            +onMouseMove(pos) void
            +render(fb) void
            +title() string
        }
    }

    namespace app {
        class AppMain {
            <<main.cpp 组装根>>
            +组装四件套并跑主循环
            +runSmokeChecks() int
        }
    }

    AppMain *-- Window
    AppMain *-- Canvas
    AppMain *-- FrameBuffer
    AppMain *-- PaintController
    Canvas ..> FrameBuffer : present 上屏
    Rasterizer ..> FrameBuffer : 只经 putPixel
    PaintController *-- "0..*" Shape : shapes_
    PaintController ..> Rasterizer : drawShape
```

### 2.2 未来接入图

未来模块按所属实验分包；`exists` 包是被接入的已实现类（只列名）。

```mermaid
classDiagram
    direction TB

    namespace exists {
        class PaintController {
            <<已实现>>
            交互中枢·图形列表
        }
        class Rasterizer {
            <<已实现>>
            drawLine 等
        }
        class FrameBuffer {
            <<已实现>>
            putPixel
        }
    }

    namespace lab2 {
        class Fill {
            <<utility·实验二>>
            +scanlineFill(fb, polygon, color)
            +scanlineSeedFill(fb, seed, color)
        }
        class Clip {
            <<utility·实验二>>
            +clipSegment(a, b, rect) Line
        }
    }

    namespace lab3 {
        class Mat3 {
            +float m[3][3]
        }
        class Transform {
            <<utility·实验三>>
            +rotateAbout(px, py, deg) Mat3
            +scaleAbout(px, py, sx, sy) Mat3
            +mul(a, b) Mat3
            +apply(m, p) FPoint
        }
    }

    namespace lab4 {
        class Bezier {
            <<utility·实验四>>
            +sampleBezier(ctrl, segments) 点列
        }
    }

    namespace lab5 {
        class LightingRenderer {
            <<选做·实验五>>
            +loadModel(path)
            +renderLambert(fb, model)
            +renderPhong(fb, model)
        }
    }

    namespace final {
        class Animation {
            <<期末项目>>
            +时间轴驱动图形重绘
        }
    }

    PaintController ..> Fill : 多边形填充交互
    PaintController ..> Clip : 裁剪命令
    Fill ..> FrameBuffer
    Clip ..> Rasterizer : 剪后重画
    PaintController ..> Transform : 选中图形施加矩阵
    Transform ..> Mat3
    PaintController ..> Bezier : 控制点编辑
    LightingRenderer ..> FrameBuffer : 软渲染逐像素
    Animation ..> PaintController : 只走公开接口
    Animation ..> Transform : 关键帧插值
```

## 3. 依赖规则（红线）

| 规则 | 内容 |
|---|---|
| 允许方向 | main → 各层；ui → raster/geom/curves/core；raster/geom/curves → core；core 不依赖任何本仓库模块 |
| 禁止反向 | 算法层（raster/geom/curves）**不得** include `src/ui` 下任何头文件 |
| OpenGL 边界 | 仅 `Canvas` 使用 OpenGL/glad（LoaderFn 由 Window 注入），其余代码与 GL 无关 |
| GLFW 边界 | 仅 `Window` 使用 GLFW；算法层经回调收到的是纯 int/float 事件 |
| 画点入口 | 一切绘制最终经 `FrameBuffer::putPixel`（课程红线：算法手写） |
| 期末项目 | Animation 只调 PaintController 公开接口驱动图形，不改引擎内部 |

## 4. 未来模块接入说明

- **实验二 Fill/Clip**：与 Rasterizer 同模式（自由函数 + FrameBuffer 参数）；PaintController 增加多边形顶点输入状态机与裁剪命令；`Shape` 扩展 `polygon` 顶点表字段。
- **实验三 Transform**：实现 Mat3 全套；PaintController 增加图形选中（点选/框选）与变换命令，参考点默认图形重心、支持任意点。**设计决策点**：线段/多边形直接变换顶点；圆/圆弧需"变换圆心 + 缩放半径"的参数化处理（届时在 issue 里定）。
- **实验四 Bezier**：de Casteljau 采样后由 PaintController 用 `raster::drawLine` 连线显示；`Shape` 扩展控制点表，控制点可拖拽实时重采样。
- **实验五（选做）LightingRenderer**：独立三维支线，仅依赖 FrameBuffer 软渲染（Lambert/Phong 逐像素），不与 2D 交互耦合；模型文件放 `assets/`。
- **期末项目 Animation**：时间轴/关键帧驱动 Shape 列表做变换，复用实验三矩阵与实验一图元；只走公开接口。

## 5. 已知架构债（后续顺手清理）

1. `Window.hpp` 为复用 `LoaderFn` 而 include 了 `Canvas.hpp`——把 LoaderFn 挪到独立小头（如 `core/Loader.hpp`）即可解开，实验二重构时处理。
2. `PaintController` 是交互中枢，实验三/四接入后职责会膨胀——届时视情况拆出 `ShapeDocument`（图形列表+选中）与工具类。
3. `Shape` 用参数化字段存圆/弧，实验三变换时需按类型分支处理（见上文设计决策点）。
